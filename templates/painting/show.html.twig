{% extends 'base.html.twig' %}

{% block title %}Painting
{% endblock %}

{% block body %}


	<body class="homeImage">
		<h1 class="editTitle">{{ painting.paintingName }}</h1>

		<div class="alertContainer mb-5">

			<div class="sizeTableAdmin">

				<table class="table">
					<tbody>
						<tr>
							<th>Id</th>
							<td>{{ painting.id }}</td>
						</tr>
						<tr>
							<th>Painting_name</th>
							<td>{{ painting.paintingName }}</td>
						</tr>
						<tr>
							<th>PaintingURL</th>
							<td>{{ painting.paintingURL }}</td>
						</tr>
						<tr>
							<th>PaintingDate</th>
							<td>{{ painting.paintingDate }}</td>
						</tr>
						<tr>
							<th>PainterName</th>
							<td>{{ painting.painterName }}</td>
						</tr>
						<tr>
							<th>PainterDescription</th>
							<td>{{ painting.painterDescription }}</td>
						</tr>
						<tr>
							<th>SmallPaintingUrl</th>
							<td>{{ painting.smallPaintingUrl }}</td>
						</tr>
					</tbody>
				</table>

				<a class="btnAdminForm mt-3" href="{{ path('app_painting_edit', {'id': painting.id}) }}">Modifier</a>

				{{ include('painting/_delete_form.html.twig') }}

				<a class="returnText" href="{{ path('app_painting_index') }}">Retour à la liste</a>

			</div>
		</div>

	</body>
{% endblock %}
