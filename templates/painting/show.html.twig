{% extends 'base.html.twig' %}

{% block title %}Painting{% endblock %}

{% block body %}
    <h1>Painting</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ painting.id }}</td>
            </tr>
            <tr>
                <th>Painting_name</th>
                <td>{{ painting.paintingName }}</td>
            </tr>
            <tr>
                <th>PaintingURL</th>
                <td>{{ painting.paintingURL }}</td>
            </tr>
            <tr>
                <th>PaintingDate</th>
                <td>{{ painting.paintingDate }}</td>
            </tr>
            <tr>
                <th>PainterName</th>
                <td>{{ painting.painterName }}</td>
            </tr>
            <tr>
                <th>PainterDescription</th>
                <td>{{ painting.painterDescription }}</td>
            </tr>
            <tr>
                <th>SmallPaintingUrl</th>
                <td>{{ painting.smallPaintingUrl }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_painting_index') }}">back to list</a>

    <a href="{{ path('app_painting_edit', {'id': painting.id}) }}">edit</a>

    {{ include('painting/_delete_form.html.twig') }}
{% endblock %}
