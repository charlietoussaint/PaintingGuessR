{% extends 'base.html.twig' %}

{% block title %}Editer la peinture
{% endblock %}

{% block body %}

	<body class="homeMain">

		<h1 class="editTitle">Editer la peinture</h1>

		<div class="alertContainer">
			<div>

				{{ include('painting/_form.html.twig', {'button_label': 'Modifier'}) }}

				{{ include('painting/_delete_form.html.twig') }}

				<a class="returnText mt-3" href="{{ path('app_painting_index') }}">Retour à la liste</a>

			</div>
		</div>

	</body>

{% endblock %}
